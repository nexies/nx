//
// Created by nexie on 24.03.2026.
//

#ifndef NX_COMMON_TIMING_HPP
#define NX_COMMON_TIMING_HPP

#include <chrono>

// C++11-compatible nested namespace (C++17 syntax: namespace nx::common_types::time)
namespace nx { namespace common_types { namespace time {
    // using hours = std::chrono::hours;
    // using minutes = std::chrono::minutes;
    // using seconds = std::chrono::seconds;
    // using milliseconds = std::chrono::milliseconds;
    // using microseconds = std::chrono::microseconds;
    // using nanoseconds = std::chrono::nanoseconds;
} } }

#endif //NX_COMMON_TIMING_HPP
